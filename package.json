{
  "name": "honkit-plugin-browser-detected-tabs",
  "version": "0.0.1",
  "description": "Tabbed browsing navigation with browser detection",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "honkit": ">1.x.x"
  },
  "honkit": {
    "properties": {
      "myConfigKey": {
        "type": "string",
        "default": "it's the default value",
        "description": "It defines my awesome config!"
      }
    }
  },
  "dependencies": {
    "detect-browser": "^5.3.0",
    "marked": "^13.0.2"
  },
  "private": true
}
